import { extendTheme } from '@chakra-ui/react';

// theme.js
const theme = extendTheme({
  colors: {
    brand: {
      300: '#f6899e',
      500: '#ef4565',
      700: '#094067',
    },
  },
});

export default theme;
